name: art-multinode

num_nodes: 2

workdir: .

resources:
  accelerators: H200:8

envs:
  PYTHONUTF8: "1"

setup: |
  uv sync --frozen --extra distributed --extra megatron

run: |
  .venv/bin/art-monarch skypilot \
    --program art.distributed.monarch_bootstrap:deployment_smoke
