<div class="art-two-column lg:sl-flex">
  <aside class="right-sidebar-container print:hidden">
    <div class="right-sidebar">
      <slot name="right-sidebar" />
    </div>
  </aside>
  <div class="main-pane"><slot /></div>
</div>

<style>
  @layer starlight.core {
    .main-pane {
      min-width: 0;
      isolation: isolate;
    }

    @media (min-width: 72rem) {
      .right-sidebar-container {
        order: 2;
        position: relative;
        flex: 0 0 var(--art-right-rail-width);
        width: var(--art-right-rail-width);
      }

      .right-sidebar {
        position: fixed;
        top: 0;
        width: var(--art-right-rail-width);
        height: 100vh;
        padding-top: var(--sl-nav-height);
        overflow-y: auto;
        scrollbar-width: none;
      }

      .main-pane {
        order: 1;
        flex: 0 0 calc(100% - var(--art-right-rail-width));
        width: calc(100% - var(--art-right-rail-width));
      }
    }
  }
</style>
